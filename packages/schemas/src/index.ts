/**
 * @workspace/schemas
 *
 * Shared Zod validation schemas for the crypto payment gateway monorepo.
 *
 * This package provides a centralized location for all validation schemas
 * that can be used across different applications (API, web, blockchain).
 */

// Export all schemas from the schemas directory
export * from './schemas/health';
